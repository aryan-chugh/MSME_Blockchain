ORACLE DASHBOARD TABLE BUTTON FIX
==================================

Replace lines 1112-1137 in OracleDashboard.js with:

                      <td>
                        {req.hasRevealed ? (
                          <button 
                            className="button" 
                            style={{ fontSize: '12px', padding: '6px 12px', background: '#9f7aea' }}
                            onClick={() => viewRequestDetails(req)}
                          >
                            ✅ Completed
                          </button>
                        ) : req.canReveal ? (
                          <button 
                            className="button" 
                            style={{ fontSize: '12px', padding: '6px 12px', background: '#9f7aea' }}
                            onClick={() => viewRequestDetails(req)}
                          >
                            🔓 Reveal Now
                          </button>
                        ) : req.hasCommitted ? (
                          <button 
                            className="button" 
                            style={{ fontSize: '12px', padding: '6px 12px', background: '#f59e0b' }}
                            onClick={() => viewRequestDetails(req)}
                          >
                            ⏳ Committed
                          </button>
                        ) : req.canCommit ? (
                          <button 
                            className="button" 
                            style={{ fontSize: '12px', padding: '6px 12px', background: '#48bb78' }}
                            onClick={() => viewRequestDetails(req)}
                          >
                            ✅ Commit Now
                          </button>
                        ) : req.isCurrentOracleAssigned && !req.isFull ? (
                          <button 
                            className="button" 
                            style={{ fontSize: '12px', padding: '6px 12px', background: '#f59e0b' }}
                            onClick={() => viewRequestDetails(req)}
                          >
                            ✓ Accepted
                          </button>
                        ) : req.isFull ? (
                          <button 
                            className="button" 
                            style={{ fontSize: '12px', padding: '6px 12px', background: '#718096', cursor: 'not-allowed' }}
                            disabled
                          >
                            Full
                          </button>
                        ) : (
                          <button 
                            className="button" 
                            style={{ fontSize: '12px', padding: '6px 12px', background: '#48bb78' }}
                            onClick={() => viewRequestDetails(req)}
                          >
                            📋 Review
                          </button>
                        )}
                      </td>
